import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  cityName: {
    textAlign: 'center',
    fontSize: 32,
  },
  currentWeatherDescription: {
    fontSize: 16,
    textAlign: 'center',
  },
  currentTemperature: {
    marginBottom: 32,
    fontSize: 48,
    textAlign: 'center',
  },
});
